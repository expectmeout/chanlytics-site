'use client'

import React from 'react';
import { motion } from 'framer-motion';

export default function Hero() {
  // Hero content moved to IntroSection component
  return (
    <div className="hidden">{ /* Hero content moved to IntroSection */ }</div>
  );
}
